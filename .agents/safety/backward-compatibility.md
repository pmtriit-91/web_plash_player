# Backward Compatibility

All changes must prioritize backward compatibility.

Check:

- Do old component props still work?
- Are old API consumers encountering errors?
- Are old states/localStorage/caches invalidated?
- Is the old UI flow missing any steps?

If backward compatibility cannot be maintained, risks must be clearly reported.
