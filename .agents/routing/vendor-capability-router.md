# Vendor capability routing view — V9.1.0

`routing/capability-registry.json` contains the only active vendor routes. A matching
trigger may select one of the eight locked engineering skills. Selection does not run
scripts, install dependencies, or grant write authority. No vendor meta-router is
enabled.
