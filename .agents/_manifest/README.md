# Release manifest area

`base-release-manifest.json` is generated by the canonical lifecycle engine and
contains only release-owned paths. It excludes Project Adapter, Project Memory,
project-local skills, and runtime output.

Files under `legacy/` preserve V8 history and old mixed-scope snapshots for forensic
reference only. They are not current configuration, routing, integrity authorities,
or proof that the referenced files still exist.
