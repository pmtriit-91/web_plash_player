# Git Commit Workflow

When writing a commit:

- Inspect the actual diff and repository history before proposing wording.
- Follow the repository's evidenced commit convention; otherwise use a concise
  imperative summary with an optional explanatory body.
- The commit message must reflect actual changes.
- Do not exaggerate the scope.
- If there are multiple unrelated change groups, propose splitting the commits.
