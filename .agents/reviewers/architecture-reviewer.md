# Architecture Reviewer

Checklist:

- Is the code in the correct layer?
- Does the component contain too much business logic?
- Is Service/API logic mixed into the UI?
- Are there duplicate types/hooks/helpers?
- Are folder/import conventions broken?
- Are there unnecessary abstractions?
