{
  "name": "web-flash-player",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "postinstall": "mkdir -p public/ruffle && cp -r node_modules/@ruffle-rs/ruffle/* public/ruffle/",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "bridge": "node server/bridge.js",
    "start": "node server/bridge.js & vite"
  },
  "dependencies": {
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@ruffle-rs/ruffle": "^0.6.0-nightly.2026.8.18",
    "vite": "^6.2.0"
  }
}
